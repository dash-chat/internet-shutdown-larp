# cargo
target/

# character private identities and posters
# (public halves live in larp-cast.toml, flashed to the cards)
secrets/
larp-identity*.toml

# nix
.direnv/
.envrc
result*

# flashing
*.img
*.log

# local bot runs (larp-run)
.run/
